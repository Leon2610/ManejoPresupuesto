

@{
    ViewData["Title"] = "Reporte Excel";

    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var yearActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var yearAnterior = haceUnMes.Year;
}

<partial name="_Submenu" Model="SubMenuTransacciones.Excel" />

<h1>Exportar Transacciones a Excel</h1>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-year="@yearActual" class="btn btn-success">
        Exportar mes actual
    </a> ~ @hoy.ToString("MMM yyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesAnterior" asp-route-year="@yearAnterior" class="btn btn-success">
        Exportar mes anterior
    </a> ~ @haceUnMes.ToString("MMM yyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-year="@yearActual" class="btn btn-success">
        Exportar año actual
    </a> ~ @hoy.ToString("MMM yyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-year="@yearActual" class="btn btn-success">
        Exportar año anterior
    </a> ~ @hoy.ToString("MMM yyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-year="@yearActual" class="btn btn-success">
        Exportar todo
    </a>
</div>